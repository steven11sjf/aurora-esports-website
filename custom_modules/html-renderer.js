// html renderer

const sheets = require('./custom_modules/spreadsheet-types'); // Uses information from new spreadsheet module
const fs = require('fs');

function renderHomePage(season) {
	
}

module.exports = {
	renderHomePage
}